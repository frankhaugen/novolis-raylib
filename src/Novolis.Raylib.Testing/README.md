# Novolis.Raylib.Testing

Optional test helpers (`RaylibOffscreenTestHarness`, `DelegateRaylibFrameRenderer`, `RaylibDebug`). Reference this package only from test projects, together with `Novolis.Raylib`.
